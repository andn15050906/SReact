Google api
900 error???
fetching
	slow at start (fetch other users)
	handle when fetches fail
	context.cs -> GetChatGroups
		hot fix: ignore reference looping
		true   : select from query
	prevent ALL api to return AppUser
chatGroup
	create success
search
file
	extension		-> a list of image extensions and video extensions
	hide path




one-to-many			-> id concat
what if
	a user delete his account while being added to a group?






fixed
	failed Login cause bug
	friendBtn if made changes

all ajax/fetch methods outside Global.js and Identity.js
	friends.js		("Identity/GetMail")
	account.js		("Identity/UpdateAccount")
	messageBox.js	("Chat/GetChatRecords")
	home.js			("Chat/GetInfo")
	contact.js		("Chat/GetOtherChatUsers")
	board.js		("Chat/UpdateNotification", "Chat/DeleteNotification")

problems
	prevent actions if the action is not accurate
	log in failed make "external login" disappear?

...
	font awesome
		check fa-regular / fa

		all i
			Nav.js
			profilePage.js		upload tag-removing tag-adding file emoji send
			messageBox.js		minimize close image emoji send file
			homeGadgets.js		bar
			Home.js				search